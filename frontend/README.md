# NeHazars Frontend

This directory contains the React + Vite UI for the NeHazars Telegram chat insights app.

## Local development
```sh
npm install
npm run dev
```

Set the API base URL in `frontend/.env` if needed:
```
VITE_API_BASE_URL=http://localhost:8000
```
